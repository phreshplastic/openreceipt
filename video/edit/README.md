# Remotion edit package

Initialize the isolated Remotion TypeScript project here after source footage exists. Keep its `package.json`, lockfile, source, cache, and render scripts inside this directory so video tooling cannot change the product application's dependency graph.

The first implementation milestone is placeholder-backed sequence compositions plus one rendered still per sequence. Read `../AGENTS.md` and `../shot-plan.json` before scaffolding.
