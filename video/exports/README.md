# Video exports

Use `stills/` for the key-frame design review, `review/` for watermarked or numbered rough cuts, and `final/` for the approved MP4, English SRT, thumbnail candidates, and `release-check.md`.

Generated exports should remain uncommitted unless Pete explicitly asks to version them.
