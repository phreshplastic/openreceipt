# Security

OpenReceipt binds to `127.0.0.1` because v1 has no accounts or remote-access authentication. Do not expose the bridge directly to a LAN or the public internet.

Report vulnerabilities privately to the repository owner. Never include printer artifacts, configuration databases, home-network details, or credentials in a public issue.
