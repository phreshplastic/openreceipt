# Interface design

Use plain, sentence-case labels and establish hierarchy with size, weight, spacing, and proximity. Reserve uppercase or monospace eyebrow labels for rare metadata that genuinely needs that treatment; overusing them is AI slop and should be avoided.
