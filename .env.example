# Optional development override. Production binds to localhost by default.
PETES_PRINTER_HOST=127.0.0.1
PETES_PRINTER_PORT=8731
# Use "dummy" to write ESC/POS output to the local app-data directory.
PETES_PRINTER_TRANSPORT=usb
